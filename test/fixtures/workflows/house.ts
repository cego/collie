/** Which implementation the modules beside this file provide. A project saving its own
 *  copy of them saves its own copy of this, which is how a concrete import is chosen. */
export const HOUSE = "strict";
