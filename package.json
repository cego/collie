{
  "name": "herdr-workflows",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.4.0"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build --compile --outfile bin/herdr-workflows.new src/main.ts && mv -f bin/herdr-workflows.new bin/herdr-workflows"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
