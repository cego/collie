bin/
node_modules/

# One-off plans, tickets and reports live in the Run directory (ADR-0002), never here.
# What belongs in git is the durable stuff: docs/, docs/adr/, CONTEXT.md.
.scratch/
plans/
advisor-plans/
